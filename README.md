This is the repository for State Management Demystified Lecture 5.

## Getting Started

First, install all dependencies by running the following command in your terminal:

```bash
pnpm i
```

Then, run the development server:

```bash
pnpm dev
```
